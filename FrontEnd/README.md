# FrontEnd (Legacy Placeholder)

Frontend source code has been migrated to `apps/web` in Phase 1.

This directory only remains temporarily because `node_modules` contained locked binaries during move on Windows.
